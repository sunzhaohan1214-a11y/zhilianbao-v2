import type { ReactNode } from "react"; import Link from "next/link"; import { notFound } from "next/navigation"; import { reimbursementPageContext } from "@/lib/reimbursement/page-context";
export default async function ReimbursementAdminLayout({children}:{children:ReactNode}){const{actor}=await reimbursementPageContext();if(!actor.hasSystem&&!actor.specialPermissions.has("reimbursement.manage"))notFound();return <div className="min-h-dvh bg-slate-100"><header className="border-b bg-white"><div className="mx-auto flex max-w-6xl items-center justify-between px-6 py-4"><div><p className="text-xs tracking-[.2em] text-blue-600">ZHILIANBAO</p><h1 className="text-xl font-semibold">报销管理</h1></div><Link href="/" className="text-sm text-blue-700">返回业务端</Link></div></header><main className="mx-auto max-w-6xl px-6 py-8">{children}</main></div>}
