import Link from "next/link"; import { notFound } from "next/navigation"; import { ReimbursementForm } from "@/components/reimbursement/reimbursement-form"; import { reimbursementPageContext } from "@/lib/reimbursement/page-context";
export default async function EditReimbursementPage({params}:{params:Promise<{id:string}>}){const{id}=await params;const{actor,service}=await reimbursementPageContext();const[item,trips]=await Promise.all([service.detail({actor,reimbursementId:id}),service.eligibleTrips({actor})]);if(item.applicantPersonId!==actor.personId||!(item.status==="DRAFT"||item.status==="RETURNED"))notFound();return <section><Link href={`/reimbursements/${id}`} className="text-sm text-blue-600">‹ 返回报销单</Link><h1 className="my-4 text-2xl font-semibold">编辑报销草稿</h1><ReimbursementForm id={id} trips={trips} initial={{type:item.type,reason:item.reason,linkedTripId:item.linkedTripId,expenses:item.expenses.map(e=>({expenseType:e.expenseType,amount:e.amount.toString(),customExpenseName:e.customExpenseName??undefined,referenceRate:e.referenceRate?.toString(),claimedDays:e.claimedDays?.toString(),description:e.description??undefined}))}}/></section>}
