import type { NextRequest } from "next/server"; import { apiError,apiSuccess } from "@/lib/api/response"; import { reimbursementRequestContext } from "@/lib/api/reimbursement-route"; import { buildAuthRequestContext } from "@/lib/auth/request-context"; import { confirmInvoiceSchema } from "@/modules/reimbursement/schemas";
export async function POST(request:NextRequest,{params}:{params:Promise<{id:string}>}){const c=buildAuthRequestContext(request);try{const{id}=await params;const{actor,context,service}=await reimbursementRequestContext(request,true);const body=confirmInvoiceSchema.parse(await request.json());return apiSuccess(await service.confirmInvoiceById({actor,context,invoiceId:id,body}),c.requestId);}catch(e){return apiError(e,c.requestId);}}
