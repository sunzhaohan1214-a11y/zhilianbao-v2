import type { NextRequest } from "next/server"; import { apiError, apiSuccess } from "@/lib/api/response"; import { mapRequestContext } from "@/lib/api/map-route"; import { buildAuthRequestContext } from "@/lib/auth/request-context"; import { enterpriseMapDetailQuerySchema } from "@/modules/map/schemas";
export async function GET(request: NextRequest, { params }: { params: Promise<{ areaId: string }> }) { const context = buildAuthRequestContext(request); try { const { actor, maps } = await mapRequestContext(request); const query = enterpriseMapDetailQuerySchema.parse(Object.fromEntries(request.nextUrl.searchParams)); return apiSuccess(await maps.enterpriseAreaDetail({ actor, context, areaId: (await params).areaId, ...query }), context.requestId); } catch (error) { return apiError(error, context.requestId); } }
