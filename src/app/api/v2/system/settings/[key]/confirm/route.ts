import type { NextRequest } from "next/server"; import { apiError, apiSuccess } from "@/lib/api/response"; import { systemRequestContext } from "@/lib/api/system-route"; import { buildAuthRequestContext } from "@/lib/auth/request-context";
export async function POST(request: NextRequest, { params }: { params: Promise<{ key: string }> }) { const context = buildAuthRequestContext(request); try { const system = await systemRequestContext(request, true); return apiSuccess(await system.settings.confirm({ actor: system.actor, context, key: decodeURIComponent((await params).key), body: await request.json(), idempotencyKey: request.headers.get("idempotency-key") }), context.requestId); } catch (error) { return apiError(error, context.requestId); } }
