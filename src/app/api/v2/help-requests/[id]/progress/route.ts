import type { NextRequest } from "next/server"; import { helpRequestContext } from "@/lib/api/help-route"; import { apiError,apiSuccess } from "@/lib/api/response"; import { buildAuthRequestContext } from "@/lib/auth/request-context"; import { addHelpProgressSchema } from "@/modules/help/schemas";
export async function POST(request:NextRequest,{params}:{params:Promise<{id:string}>}){const context=buildAuthRequestContext(request);try{const{actor,service}=await helpRequestContext(request,true);const body=addHelpProgressSchema.parse(await request.json());return apiSuccess(await service.addProgress({actor,context,helpRequestId:(await params).id,body}),context.requestId,201);}catch(error){return apiError(error,context.requestId);}}
