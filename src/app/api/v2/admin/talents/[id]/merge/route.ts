import type { NextRequest } from "next/server"; import { apiError,apiSuccess } from "@/lib/api/response"; import { talentRequestContext } from "@/lib/api/talent-route"; import { buildAuthRequestContext } from "@/lib/auth/request-context"; import { mergeTalentSchema } from "@/modules/talent/schemas";
export async function POST(request:NextRequest,route:{params:Promise<{id:string}>}){const context=buildAuthRequestContext(request);try{const{actor,service}=await talentRequestContext(request,true);const{id}=await route.params;const body=mergeTalentSchema.parse(await request.json());return apiSuccess(await service.merge({actor,context,talentId:id,targetTalentId:body.targetTalentId,reason:body.reason}),context.requestId);}catch(error){return apiError(error,context.requestId);}}
