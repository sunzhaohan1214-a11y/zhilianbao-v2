import type { NextRequest } from "next/server"; import { apiError } from "@/lib/api/response"; import { importExportRequestContext } from "@/lib/api/import-export-route"; import { buildAuthRequestContext } from "@/lib/auth/request-context";
export async function POST(request: NextRequest) { const context = buildAuthRequestContext(request); try { const { actor, exportService } = await importExportRequestContext(request, true); const result = await exportService.talent({ actor, context, body: await request.json() }); return new Response(result.buffer, { headers: { "content-type": "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", "content-disposition": `attachment; filename="${result.filename}"`, "x-export-row-count": String(result.rowCount), "cache-control": "private, no-store" } }); } catch (error) { return apiError(error, context.requestId); } }
