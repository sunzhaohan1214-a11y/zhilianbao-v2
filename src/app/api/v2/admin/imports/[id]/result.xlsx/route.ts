import type { NextRequest } from "next/server"; import { apiError } from "@/lib/api/response"; import { importExportRequestContext } from "@/lib/api/import-export-route"; import { buildAuthRequestContext } from "@/lib/auth/request-context";
export async function GET(request: NextRequest, { params }: { params: Promise<{ id: string }> }) { const context = buildAuthRequestContext(request); try { const { id } = await params; const { actor, importService } = await importExportRequestContext(request); const result = await importService.resultWorkbook({ actor, context, batchId: id }); return new Response(new Uint8Array(result.buffer), { headers: { "content-type": "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", "content-disposition": `attachment; filename="${result.filename}"`, "cache-control": "private, no-store" } }); } catch (error) { return apiError(error, context.requestId); } }
