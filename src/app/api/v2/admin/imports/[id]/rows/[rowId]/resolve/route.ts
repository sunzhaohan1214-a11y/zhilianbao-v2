import type { NextRequest } from "next/server"; import { apiError, apiSuccess } from "@/lib/api/response"; import { importExportRequestContext } from "@/lib/api/import-export-route"; import { buildAuthRequestContext } from "@/lib/auth/request-context";
export async function POST(request: NextRequest, { params }: { params: Promise<{ id: string; rowId: string }> }) { const context = buildAuthRequestContext(request); try { const { id, rowId } = await params; const { actor, importService } = await importExportRequestContext(request, true); return apiSuccess(await importService.resolveRow({ actor, context, batchId: id, rowId, body: await request.json() }), context.requestId); } catch (error) { return apiError(error, context.requestId); } }
