import type { NextRequest } from "next/server"; import { apiError, apiSuccess } from "@/lib/api/response"; import { reimbursementRequestContext } from "@/lib/api/reimbursement-route"; import { buildAuthRequestContext } from "@/lib/auth/request-context"; import { ReimbursementError } from "@/modules/reimbursement/errors";
export async function GET(request: NextRequest, { params }: { params: Promise<{ id: string }> }) { const c = buildAuthRequestContext(request); try { const { id } = await params; const { actor, context, service } = await reimbursementRequestContext(request); if (!actor.hasSystem && !actor.specialPermissions.has("reimbursement.manage")) throw new ReimbursementError("REIMBURSEMENT_NOT_FOUND", "报销单不存在或无权查看"); return apiSuccess(await service.detail({ actor, context, reimbursementId: id }), c.requestId); } catch (e) { return apiError(e, c.requestId); } }
