import type { NextRequest } from "next/server"; import { apiError, apiSuccess } from "@/lib/api/response"; import { mapRequestContext } from "@/lib/api/map-route"; import { buildAuthRequestContext } from "@/lib/auth/request-context";
export async function GET(request: NextRequest, { params }: { params: Promise<{ id: string }> }) { const context = buildAuthRequestContext(request); try { const { actor, maps } = await mapRequestContext(request); return apiSuccess(await maps.activationPreview({ actor, context, boundaryId: (await params).id }), context.requestId); } catch (error) { return apiError(error, context.requestId); } }
export async function POST(request: NextRequest, { params }: { params: Promise<{ id: string }> }) { const context = buildAuthRequestContext(request); try { const { actor, maps } = await mapRequestContext(request, true); return apiSuccess(await maps.activateBoundary({ actor, context, boundaryId: (await params).id, command: await request.json(), idempotencyKey: request.headers.get("idempotency-key") }), context.requestId); } catch (error) { return apiError(error, context.requestId); } }
