import type { NextRequest } from "next/server"; import { announcementRequestContext } from "@/lib/api/announcement-route"; import { apiError,apiSuccess } from "@/lib/api/response"; import { buildAuthRequestContext } from "@/lib/auth/request-context";
export async function GET(request:NextRequest,{params}:{params:Promise<{id:string}>}){const context=buildAuthRequestContext(request);try{const{actor,service}=await announcementRequestContext(request);return apiSuccess(await service.confirmationStatus({actor,context,announcementId:(await params).id}),context.requestId);}catch(error){return apiError(error,context.requestId);}}
