import type { NextRequest } from "next/server"; import { announcementRequestContext } from "@/lib/api/announcement-route"; import { apiError,apiSuccess } from "@/lib/api/response"; import { buildAuthRequestContext } from "@/lib/auth/request-context";
export async function POST(request:NextRequest,{params}:{params:Promise<{id:string}>}){const context=buildAuthRequestContext(request);try{const{actor,service}=await announcementRequestContext(request,true);return apiSuccess(await service.updateAudience({actor,context,announcementId:(await params).id,body:await request.json()}),context.requestId);}catch(error){return apiError(error,context.requestId);}}
