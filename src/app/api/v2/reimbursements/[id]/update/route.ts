import type { NextRequest } from "next/server"; import { apiError, apiSuccess } from "@/lib/api/response"; import { reimbursementRequestContext } from "@/lib/api/reimbursement-route"; import { buildAuthRequestContext } from "@/lib/auth/request-context"; import { reimbursementDraftSchema } from "@/modules/reimbursement/schemas";
export async function POST(request: NextRequest,{params}:{params:Promise<{id:string}>}){const c=buildAuthRequestContext(request);try{const{id}=await params;const{actor,context,service}=await reimbursementRequestContext(request,true);const body=reimbursementDraftSchema.parse(await request.json());return apiSuccess(await service.update({actor,context,reimbursementId:id,body}),c.requestId);}catch(e){return apiError(e,c.requestId);}}
