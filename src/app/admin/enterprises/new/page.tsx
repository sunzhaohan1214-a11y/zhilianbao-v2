import Link from "next/link"; import { AdminEnterpriseCreateForm } from "@/components/enterprise/admin-enterprise-forms"; import { enterprisePageContext } from "@/lib/enterprise/page-context";
export default async function NewEnterprisePage() { const { actor, service } = await enterprisePageContext(); const options = await service.formOptions({ actor, purpose: "FORMAL_CREATE" }); return <section><Link href="/admin/enterprises" className="text-sm text-blue-600">‹ 返回企业管理</Link><h1 className="my-5 text-3xl font-semibold">企业正式建档</h1><AdminEnterpriseCreateForm areas={options.areas} tags={options.tags}/></section>; }
