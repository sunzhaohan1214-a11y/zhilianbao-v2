import type { ProviderHealth } from "./backup-provider";
export interface MaintenanceProvider { health(): Promise<ProviderHealth>; status(): Promise<{ active: boolean; operationId?: string }>; enter(input: { operationId: string; reason: string }): Promise<void>; exit(operationId: string): Promise<void>; }
export class UnavailableMaintenanceProvider implements MaintenanceProvider { async health(): Promise<ProviderHealth> { return { ready: false, status: "NOT_CONFIGURED", provider: "unavailable-maintenance", detail: "未配置部署层持久写锁" }; } async status() { return { active: false }; } async enter(): Promise<never> { throw new Error("RESTORE_MAINTENANCE_UNAVAILABLE"); } async exit(): Promise<never> { throw new Error("RESTORE_MAINTENANCE_UNAVAILABLE"); } }
export class FakeMaintenanceProvider implements MaintenanceProvider { private current: string | undefined; async health(): Promise<ProviderHealth> { return { ready: true, status: "READY", provider: "fake-maintenance" }; } async status() { return { active: Boolean(this.current), operationId: this.current }; } async enter(input: { operationId: string; reason: string }) { if (this.current && this.current !== input.operationId) throw new Error("MAINTENANCE_ALREADY_ACTIVE"); this.current = input.operationId; } async exit(operationId: string) { if (this.current !== operationId) throw new Error("MAINTENANCE_OPERATION_MISMATCH"); this.current = undefined; } }
const runtime = globalThis as typeof globalThis & { __zlbMaintenanceProvider?: MaintenanceProvider };
export function getMaintenanceProvider(): MaintenanceProvider { runtime.__zlbMaintenanceProvider ??= process.env.APP_ENV === "test" || process.env.NODE_ENV === "test" ? new FakeMaintenanceProvider() : new UnavailableMaintenanceProvider(); return runtime.__zlbMaintenanceProvider; }
